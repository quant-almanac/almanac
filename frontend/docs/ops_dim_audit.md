# OPS.dim コントラスト監査（Phase 4）

> **解決（デザイン磨きパス）**: トークン値自体を `#6C6960`（3.5:1）→ `#807C73`（4.7:1）に
> 引き上げた。218箇所の呼び出しは無変更のまま全箇所でコントラストが改善し、
> `OPS.sub`（6.7:1）との階層差も維持されている。下表の「本文候補12領域」の
> 個別 `sub` 昇格は不要になったため実施しない。

対象は `#6C6960`（OPS.dim）を `#0B0D12` 背景で使う箇所。検索時点で218件あり、色の一括変更は行わない。

## 判定基準

- 維持: 表ヘッダー、時刻、単位、区切り、チャート軸、空状態、補助ラベル。
- 次回の色調整候補: 意味を読む本文、理由、説明文で、12px以上または複数行になる箇所。
- 既存の `OPS.sub` への変更は、次回デザインレビューで文章ごとに判断する。現フェーズではデータの重要度・状態色を変えない。

## 本文候補（次回レビュー対象）

| ファイル | 表示内容 | 現状 |
|---|---|---|
| `app/decision/page.tsx` | ケースの説明、空状態の最終判断説明 | 14px |
| `app/agent/page.tsx` | 自動実行時刻の説明 | 14px |
| `app/risk/page.tsx` | リスク指標の説明 | 11.5px、複数行 |
| `app/strategy/page.tsx` | 期待超過リターンと信念カードの説明 | 11px、複数行 |
| `app/tuning/page.tsx` | パラメータ説明 | 12px、複数行 |
| `app/admin/page.tsx` | 保有メモ | 11.5px、複数行 |
| `components/today/ops/AnalystSection.tsx` | 分析・根拠・レーンの説明 | 12〜13px、複数行 |
| `components/today/ops/TrustSection.tsx` | 配分・信頼性の説明 | 11.5〜12px、複数行 |
| `components/today/ops/PlanRail.tsx` | 計画枠の補足 | 11px |
| `components/today/ops/ShortSignalsPanel.tsx` | シグナル理由 | 11.5px |
| `components/today/ops/SignalMap.tsx` | シグナル変化とフッター | 11.5〜12px |
| `components/today/ops/OrderMap.tsx` | マップの補足 | 11px |

## 監査結果

- 218件中、表・時刻・単位・空状態・区切り・軸などのメタデータ用途が大半。
- 本文候補は検索上69件あるが、同一コンポーネントの繰返し表示を含む。上表の12領域を色調整の判断単位とする。
- 今回はアクセント、状態、発注・ガード・実行計画のセマンティクスを変えないため、`OPS.dim` の値および呼び出し箇所は変更しない。
